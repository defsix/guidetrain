<resources>
    <!-- Matches the CSS custom property "bg" (light theme) from apps/web/src/index.css. -->
    <color name="window_background">#F2F4F7</color>
    <!-- Matches the CSS custom property "bg" (dark theme) from apps/web/src/index.css. -->
    <color name="window_background_dark">#0B0D12</color>
    <color name="ic_launcher_background">#0B0D12</color>
    <!-- Matches the CSS custom property "accent" (dark theme) from apps/web/src/index.css. -->
    <color name="ic_launcher_foreground">#F97316</color>
</resources>
